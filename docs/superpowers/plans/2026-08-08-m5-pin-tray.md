# M5 贴屏 + 托盘完善 + 单实例 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 截图/标注后点「贴到屏幕」以物理像素 1:1 悬浮置顶（可拖动/缩放/透明度/鼠标穿透、多张并存点击置顶），完成托盘菜单完善与单实例 Mutex。

**Architecture:** 方案 A——WPF 无边框窗口（`AllowsTransparency` + `Topmost`），穿透用 `SetWindowLongPtr(GWL_EXSTYLE)` 增删 `WS_EX_TRANSPARENT`，透明度用 WPF `Window.Opacity`，缩放用 `Width/Height` + `Image Stretch=Fill`。纯逻辑 `PinMath`（像素↔布局换算、缩放钳制）TDD 先行。App 持有 `List<PinWindow>` 防 GC，`Mutex` 单实例，热键复用 `KeyboardHookService`。

**Tech Stack:** C# 14 / WPF / .NET 10 / xUnit（沿用现有项目栈，零第三方依赖）。

## Global Constraints

- 坐标体系：截图与 Win32 几何一律用**虚拟屏幕物理像素**；WPF 布局坐标 = 物理像素 / 显示器 DpiScale（`VisualTreeHelper.GetDpi`，项目已有模式）
- 纯逻辑 TDD 先行（项目规则 4）：PinMath 必须先写测试再实现（red→green）
- 剪贴板写入走 `ClipboardEx`（DIB+PNG 双格式，STA 线程）、保存走 `ImageFile.SavePngWithDialog`——不得新写编码逻辑
- 全局类型别名：WPF/WinForms 二义性优先在 `GlobalUsings.cs` 加别名，不写全限定
- 命名空间避开常用类型同名（如已改 `ClipboardServices`）
- 分支合并/远程删除绝不允许（规则 9）；提交前同步文档（规则 10）
- 构建验证：每个任务结束 `dotnet build EasySnipLite.slnx` + `dotnet test EasySnipLite.slnx` 全绿后再提交

---

### Task 1: KeyEvent 扩展 ShiftDown + Win32 补充声明

穿透热键 Ctrl+Shift+P 需要 Shift 状态，但 `KeyEvent` record 只有 `CtrlDown`。同时 Win32 需要 `VK_SHIFT` 与 `SetWindowPos`（WS_EX_TRANSPARENT 生效需 `SWP_FRAMECHANGED` 刷新）。

**Files:**
- Modify: `src/EasySnipLite/Core/Hotkeys/ChordDetector.cs:9`
- Modify: `src/EasySnipLite/Core/Hotkeys/KeyboardHookService.cs:54-58`
- Modify: `src/EasySnipLite/Core/Native/Win32.cs`（文件末尾追加）
- Test: `tests/EasySnipLite.Tests/ChordDetectorTests.cs:11-18`

**Interfaces:**
- Consumes: 现有 `Win32.GetAsyncKeyState(int)`、`Win32.VK_CONTROL`
- Produces: `KeyEvent(KeyEventType Type, int VirtualKey, bool CtrlDown, bool ShiftDown, DateTime Timestamp)`（record，位置参数新增第 4 个）；`Win32.VK_SHIFT = 0x10`；`Win32.SetWindowPos(...)` 声明与 `SWP_NOMOVE/SWP_NOSIZE/SWP_NOZORDER/SWP_FRAMECHANGED` 常量

- [ ] **Step 1: 修改 `KeyEvent` record 加 `ShiftDown`**

`src/EasySnipLite/Core/Hotkeys/ChordDetector.cs` 第 9 行改为：

```csharp
public sealed record KeyEvent(KeyEventType Type, int VirtualKey, bool CtrlDown, bool ShiftDown, DateTime Timestamp);
```

（`ChordDetector` 类体不动——它只按属性名访问 `e.CtrlDown`/`e.Timestamp`，不受影响。）

- [ ] **Step 2: 更新钩子回调构造事件（带 Shift 状态）**

`src/EasySnipLite/Core/Hotkeys/KeyboardHookService.cs` 的 `Callback` 方法中，把 `bool ctrlDown = ...` 行之后改为：

```csharp
                bool ctrlDown = (Win32.GetAsyncKeyState(Win32.VK_CONTROL) & 0x8000) != 0;
                bool shiftDown = (Win32.GetAsyncKeyState(Win32.VK_SHIFT) & 0x8000) != 0;
                bool isUp = msg is Win32.WM_KEYUP or Win32.WM_SYSKEYUP;
                var evt = new KeyEvent(
                    isUp ? KeyEventType.KeyUp : KeyEventType.KeyDown,
                    (int)data.vkCode,
                    ctrlDown,
                    shiftDown,
                    DateTime.UtcNow);
```

- [ ] **Step 3: Win32 追加 VK_SHIFT 与 SetWindowPos 声明**

`src/EasySnipLite/Core/Native/Win32.cs` 文件末尾（`SetCursorPos` 之后）追加：

```csharp
    // ---- 贴屏热键与窗口刷新（M5 用） ----
    public const int VK_SHIFT = 0x10;

    public const uint SWP_NOSIZE = 0x0001;
    public const uint SWP_NOMOVE = 0x0002;
    public const uint SWP_NOZORDER = 0x0004;
    public const uint SWP_FRAMECHANGED = 0x0020;

    [DllImport("user32.dll")]
    public static extern bool SetWindowPos(IntPtr hWnd, IntPtr hWndInsertAfter, int X, int Y, int cx, int cy, uint uFlags);
```

- [ ] **Step 4: 更新 ChordDetectorTests 的 KeyEvent 构造（加第 4 个实参）**

`tests/EasySnipLite.Tests/ChordDetectorTests.cs` 三个 helper 改为：

```csharp
    private static KeyEvent SpaceUp(bool ctrl, DateTime t) =>
        new(KeyEventType.KeyUp, VkSpace, ctrl, false, t);

    private static KeyEvent SpaceDown(DateTime t) =>
        new(KeyEventType.KeyDown, VkSpace, true, false, t);

    private static KeyEvent OtherUp(DateTime t) =>
        new(KeyEventType.KeyUp, VkShift, true, false, t);
```

- [ ] **Step 5: 构建并运行全部测试**

Run: `dotnet build EasySnipLite.slnx`
Expected: 0 错误 0 警告

Run: `dotnet test EasySnipLite.slnx`
Expected: 111/111 全绿（ChordDetector 7 个用例含 `OtherKeysBetweenPresses_DoNotInterfere` 不受新参数影响）

- [ ] **Step 6: 提交**

```bash
git add src/EasySnipLite/Core/Native/Win32.cs src/EasySnipLite/Core/Hotkeys/ChordDetector.cs src/EasySnipLite/Core/Hotkeys/KeyboardHookService.cs tests/EasySnipLite.Tests/ChordDetectorTests.cs
git commit -m "feat: KeyEvent 增加 ShiftDown 状态(贴屏穿透热键 Ctrl+Shift+P 用)+Win32 SetWindowPos/VK_SHIFT 声明"
```

---

### Task 2: PinMath 纯逻辑 TDD（像素↔布局换算 + 缩放钳制）

**Files:**
- Create: `src/EasySnipLite/Pin/PinMath.cs`
- Test: `tests/EasySnipLite.Tests/PinMathTests.cs`

**Interfaces:**
- Consumes: 无（纯函数）
- Produces:
  - `public static class PinMath`
  - `public const double MinZoom = 0.5;` / `public const double MaxZoom = 3.0;` / `public const double ZoomStep = 1.1;`
  - `public static (double Width, double Height) LayoutSize(int pixelW, int pixelH, double dpiScale, double zoom)` — `(pixelW / dpiScale * zoom, pixelH / dpiScale * zoom)`
  - `public static (double X, double Y) LayoutPosition(int pixelX, int pixelY, double dpiScale)` — `(pixelX / dpiScale, pixelY / dpiScale)`
  - `public static double NextZoom(double current, bool zoomIn)` — 乘/除 `ZoomStep`，`Math.Clamp(next, MinZoom, MaxZoom)`

- [ ] **Step 1: 写失败测试**

`tests/EasySnipLite.Tests/PinMathTests.cs`（新文件）：

```csharp
using EasySnipLite.Pin;

namespace EasySnipLite.Tests;

public class PinMathTests
{
    // ---- 1:1 布局换算 ----

    [Fact]
    public void LayoutSize_Dpi100_IsPixelSize()
    {
        var (w, h) = PinMath.LayoutSize(300, 200, 1.0, 1.0);
        Assert.Equal(300, w, 3);
        Assert.Equal(200, h, 3);
    }

    [Fact]
    public void LayoutSize_Dpi150_ShrinksToLayoutUnits()
    {
        var (w, h) = PinMath.LayoutSize(300, 200, 1.5, 1.0);
        Assert.Equal(200, w, 3);
        Assert.Equal(200 / 1.5, h, 3);
    }

    [Fact]
    public void LayoutSize_Zoom2_DoublesLayout()
    {
        var (w, h) = PinMath.LayoutSize(300, 200, 1.0, 2.0);
        Assert.Equal(600, w, 3);
        Assert.Equal(400, h, 3);
    }

    [Fact]
    public void LayoutSize_Dpi150_Zoom05_Combined()
    {
        var (w, h) = PinMath.LayoutSize(300, 200, 1.5, 0.5);
        Assert.Equal(100, w, 3);
        Assert.Equal(200.0 / 3.0, h, 3);
    }

    [Fact]
    public void LayoutPosition_Dpi100_IsPixelCoords()
    {
        var (x, y) = PinMath.LayoutPosition(1920, 1080, 1.0);
        Assert.Equal(1920, x, 3);
        Assert.Equal(1080, y, 3);
    }

    [Fact]
    public void LayoutPosition_Dpi150_DividesByScale()
    {
        var (x, y) = PinMath.LayoutPosition(1920, 1080, 1.5);
        Assert.Equal(1280, x, 3);
        Assert.Equal(720, y, 3);
    }

    // ---- 缩放步进与钳制 ----

    [Fact]
    public void NextZoom_ZoomIn_MultipliesByStep()
    {
        Assert.Equal(1.1, PinMath.NextZoom(1.0, zoomIn: true), 6);
        Assert.Equal(2.0 * 1.1, PinMath.NextZoom(2.0, zoomIn: true), 6);
    }

    [Fact]
    public void NextZoom_ZoomOut_DividesByStep()
    {
        Assert.Equal(1.0, PinMath.NextZoom(1.1, zoomIn: false), 6);
        Assert.Equal(2.0 / 1.1, PinMath.NextZoom(2.0, zoomIn: false), 6);
    }

    [Fact]
    public void NextZoom_ClampsAtMax()
    {
        Assert.Equal(3.0, PinMath.NextZoom(3.0, zoomIn: true), 6);
        Assert.Equal(3.0, PinMath.NextZoom(2.9, zoomIn: true), 6);
    }

    [Fact]
    public void NextZoom_ClampsAtMin()
    {
        Assert.Equal(0.5, PinMath.NextZoom(0.5, zoomIn: false), 6);
        Assert.Equal(0.5, PinMath.NextZoom(0.51, zoomIn: false), 6);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `dotnet test --filter "FullyQualifiedName~PinMathTests"`
Expected: FAIL（`PinMath` 类型不存在）

- [ ] **Step 3: 实现 PinMath**

`src/EasySnipLite/Pin/PinMath.cs`（新文件，目录自动创建）：

```csharp
namespace EasySnipLite.Pin;

/// <summary>
/// 贴屏窗口的纯逻辑换算：物理像素 ↔ WPF 布局坐标（÷DpiScale）、缩放步进与钳制。
/// 布局尺寸 = 物理像素 / DpiScale × zoom；窗口位置 = 物理像素 / DpiScale。
/// </summary>
public static class PinMath
{
    public const double MinZoom = 0.5;
    public const double MaxZoom = 3.0;
    public const double ZoomStep = 1.1;

    public static (double Width, double Height) LayoutSize(
        int pixelW, int pixelH, double dpiScale, double zoom) =>
        (pixelW / dpiScale * zoom, pixelH / dpiScale * zoom);

    public static (double X, double Y) LayoutPosition(int pixelX, int pixelY, double dpiScale) =>
        (pixelX / dpiScale, pixelY / dpiScale);

    public static double NextZoom(double current, bool zoomIn)
    {
        var next = zoomIn ? current * ZoomStep : current / ZoomStep;
        return Math.Clamp(next, MinZoom, MaxZoom);
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `dotnet test --filter "FullyQualifiedName~PinMathTests"`
Expected: 10/10 PASS

- [ ] **Step 5: 提交**

```bash
git add src/EasySnipLite/Pin/PinMath.cs tests/EasySnipLite.Tests/PinMathTests.cs
git commit -m "feat: PinMath 贴屏换算纯逻辑(1:1 布局/DPI/缩放钳制,TDD 10 单测)"
```

---

### Task 3: PinWindow 贴屏窗口（WPF 无边框 + WS_EX_TRANSPARENT 穿透）

**Files:**
- Create: `src/EasySnipLite/Pin/PinWindow.xaml`
- Create: `src/EasySnipLite/Pin/PinWindow.xaml.cs`

**Interfaces:**
- Consumes: `PinMath`（Task 2）、`Win32`（Task 1：`GWL_EXSTYLE`/`WS_EX_TRANSPARENT`/`GetWindowLongPtr`/`SetWindowLongPtr`/`SetWindowPos`/`SWP_*`）、`ClipboardEx.SetImage(BitmapSource)`、`ImageFile.SavePngWithDialog(BitmapSource, string)`（均已存在）
- Produces:
  - `public PinWindow(BitmapSource image, int pixelX, int pixelY)` — 构造后 `Show()` 即按 1:1 显示在截图原位置
  - `public bool IsPassthrough { get; set; }` — 穿透状态（setter 即应用 EXSTYLE）
  - 继承 `Window`；`Close()` 由用户触发

- [ ] **Step 1: 写 XAML**

`src/EasySnipLite/Pin/PinWindow.xaml`（新文件）：

```xml
<Window x:Class="EasySnipLite.Pin.PinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Topmost="True"
        ShowInTaskbar="False" ResizeMode="NoResize" Background="Transparent"
        PreviewMouseWheel="OnPreviewMouseWheel"
        PreviewMouseLeftButtonDown="OnPreviewMouseLeftButtonDown">
    <Border x:Name="Frame" BorderBrush="#FF7A7A82" BorderThickness="1">
        <Image x:Name="PinImage" Stretch="Fill"/>
    </Border>
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="鼠标穿透" IsCheckable="True" Checked="Passthrough_Toggled" Unchecked="Passthrough_Toggled"/>
            <MenuItem Header="透明度">
                <MenuItem Header="100%" Tag="1.0" Click="Opacity_Click"/>
                <MenuItem Header="85%" Tag="0.85" Click="Opacity_Click"/>
                <MenuItem Header="70%" Tag="0.7" Click="Opacity_Click"/>
                <MenuItem Header="50%" Tag="0.5" Click="Opacity_Click"/>
            </MenuItem>
            <MenuItem Header="100% 缩放" Click="Zoom100_Click"/>
            <Separator/>
            <MenuItem Header="复制" Click="Copy_Click"/>
            <MenuItem Header="保存" Click="Save_Click"/>
            <MenuItem Header="关闭" Click="Close_Click"/>
        </ContextMenu>
    </Window.ContextMenu>
</Window>
```

- [ ] **Step 2: 写 code-behind**

`src/EasySnipLite/Pin/PinWindow.xaml.cs`（新文件）：

```csharp
using System.Windows;
using System.Windows.Input;
using System.Windows.Interop;
using System.Windows.Media;
using System.Windows.Media.Imaging;
using EasySnipLite.Core.ClipboardServices;
using EasySnipLite.Core.Imaging;
using EasySnipLite.Core.Native;

namespace EasySnipLite.Pin;

/// <summary>
/// 贴屏窗口：无边框置顶，初始 1:1（物理像素/DpiScale）显示在截图原位置。
/// 拖动=左键 DragMove；Ctrl+滚轮缩放（50%~300%）；右键菜单：穿透/透明度/100% 缩放/复制/保存/关闭。
/// 穿透=SetWindowLongPtr 增删 WS_EX_TRANSPARENT（WPF AllowsTransparency 窗口已是 layered，透明度走 Window.Opacity）。
/// 多张并存：App 持有列表管理，Topmost 组内点击激活即置前。
/// </summary>
public partial class PinWindow : Window
{
    private readonly BitmapSource _image; // 物理像素原图（复制/保存/重算布局用）
    private readonly int _pixelX;
    private readonly int _pixelY;
    private double _dpiScale = 1.0;
    private double _zoom = 1.0;
    private bool _passthrough;

    public PinWindow(BitmapSource image, int pixelX, int pixelY)
    {
        InitializeComponent();
        _image = image;
        _pixelX = pixelX;
        _pixelY = pixelY;
        PinImage.Source = image;
        Loaded += OnLoaded;
    }

    public bool IsPassthrough
    {
        get => _passthrough;
        set
        {
            if (_passthrough == value) return;
            _passthrough = value;
            ApplyPassthrough();
        }
    }

    private void OnLoaded(object sender, RoutedEventArgs e)
    {
        _dpiScale = VisualTreeHelper.GetDpi(this).DpiScaleX;
        ApplyLayout();
    }

    /// <summary>窗口尺寸/位置 = 物理像素 / DpiScale（×zoom）。</summary>
    private void ApplyLayout()
    {
        var (w, h) = PinMath.LayoutSize(_image.PixelWidth, _image.PixelHeight, _dpiScale, _zoom);
        var (x, y) = PinMath.LayoutPosition(_pixelX, _pixelY, _dpiScale);
        Width = w;
        Height = h;
        Left = x;
        Top = y;
    }

    /// <summary>切换 WS_EX_TRANSPARENT（穿透）。SWP_FRAMECHANGED 强制刷新扩展样式生效。</summary>
    private void ApplyPassthrough()
    {
        var hwnd = new WindowInteropHelper(this).Handle;
        var style = Win32.GetWindowLongPtr(hwnd, Win32.GWL_EXSTYLE).ToInt64();
        style = _passthrough ? style | Win32.WS_EX_TRANSPARENT : style & ~Win32.WS_EX_TRANSPARENT;
        Win32.SetWindowLongPtr(hwnd, Win32.GWL_EXSTYLE, new IntPtr(style));
        Win32.SetWindowPos(hwnd, IntPtr.Zero, 0, 0, 0, 0,
            Win32.SWP_NOMOVE | Win32.SWP_NOSIZE | Win32.SWP_NOZORDER | Win32.SWP_FRAMECHANGED);
    }

    // ---- 交互 ----

    private void OnPreviewMouseLeftButtonDown(object sender, MouseButtonEventArgs e)
    {
        if (e.ButtonState == MouseButtonState.Pressed)
            DragMove();
    }

    private void OnPreviewMouseWheel(object sender, MouseWheelEventArgs e)
    {
        if ((Keyboard.Modifiers & ModifierKeys.Control) == 0) return;
        _zoom = PinMath.NextZoom(_zoom, e.Delta > 0);
        ApplyLayout();
        e.Handled = true;
    }

    // ---- 右键菜单 ----

    private void Passthrough_Toggled(object sender, RoutedEventArgs e)
    {
        // IsChecked 是 bool?，用 bool 类型模式解包（null 不匹配）
        if (sender is System.Windows.Controls.MenuItem { IsChecked: bool isChecked })
            IsPassthrough = isChecked;
    }

    private void Opacity_Click(object sender, RoutedEventArgs e)
    {
        if (sender is System.Windows.Controls.MenuItem { Tag: string tag } && double.TryParse(tag, out var alpha))
            Opacity = alpha;
    }

    private void Zoom100_Click(object sender, RoutedEventArgs e)
    {
        _zoom = 1.0;
        ApplyLayout();
    }

    private void Copy_Click(object sender, RoutedEventArgs e)
    {
        try
        {
            ClipboardEx.SetImage(_image);
        }
        catch (Exception ex)
        {
            MessageBox.Show(this, $"复制失败：{ex.Message}", "EasySnipLite", MessageBoxButton.OK, MessageBoxImage.Error);
        }
    }

    private void Save_Click(object sender, RoutedEventArgs e)
    {
        try
        {
            ImageFile.SavePngWithDialog(_image, ImageFile.DefaultFileName());
        }
        catch (Exception ex)
        {
            MessageBox.Show(this, $"保存失败：{ex.Message}", "EasySnipLite", MessageBoxButton.OK, MessageBoxImage.Error);
        }
    }

    private void Close_Click(object sender, RoutedEventArgs e) => Close();
}
```

- [ ] **Step 3: 构建**

Run: `dotnet build EasySnipLite.slnx`
Expected: 0 错误 0 警告（`PinMath` 引用来自 Task 2）

- [ ] **Step 4: 提交**

```bash
git add src/EasySnipLite/Pin/PinWindow.xaml src/EasySnipLite/Pin/PinWindow.xaml.cs
git commit -m "feat: PinWindow 贴屏窗口(1:1 置顶/拖动/Ctrl+滚轮缩放/右键菜单/WS_EX_TRANSPARENT 穿透)"
```

---

### Task 4: 编辑器动作条「贴到屏幕」入口

**Files:**
- Modify: `src/EasySnipLite/Editor/EditorWindow.xaml:56-58`（动作条区）
- Modify: `src/EasySnipLite/Editor/EditorWindow.xaml.cs`（动作条处理区 + 类首字段）

**Interfaces:**
- Consumes: `EditorViewModel.Compose()`（已有）
- Produces: `public event Action<BitmapSource>? PinRequested;` — 点击「贴到屏幕」触发：携带 Compose 结果并 `Close()`

- [ ] **Step 1: XAML 动作条加按钮**

`src/EasySnipLite/Editor/EditorWindow.xaml`，在「保存」按钮与「完成」按钮之间插入：

```xml
                <Button Content="贴到屏幕" Click="Pin_Click" Margin="0,0,3,0" ToolTip="贴到屏幕"/>
```

- [ ] **Step 2: code-behind 加事件与处理**

`src/EasySnipLite/Editor/EditorWindow.xaml.cs`：

类字段区（`private Point _emojiPoint;` 之后）加：

```csharp
    /// <summary>「贴到屏幕」：携带组合位图，由 App 打开贴屏窗口（本窗口随即关闭）。</summary>
    public event Action<BitmapSource>? PinRequested;
```

动作条区（`Complete_Click` 方法附近）加：

```csharp
    private void Pin_Click(object sender, RoutedEventArgs e)
    {
        PinRequested?.Invoke(_vm.Compose());
        Close();
    }
```

- [ ] **Step 3: 构建**

Run: `dotnet build EasySnipLite.slnx`
Expected: 0 错误 0 警告（事件尚未被订阅，不影响编译）

- [ ] **Step 4: 提交**

```bash
git add src/EasySnipLite/Editor/EditorWindow.xaml src/EasySnipLite/Editor/EditorWindow.xaml.cs
git commit -m "feat: 编辑器动作条新增「贴到屏幕」(PinRequested 事件,贴屏后关编辑器)"
```

---

### Task 5: App 贴屏装配（OpenPin / _pins / 热键 / region 传递）

**Files:**
- Modify: `src/EasySnipLite/App.xaml.cs`（多处：字段区、`OnKey`、`StartCapture`、`OpenEditor`）

**Interfaces:**
- Consumes: `PinWindow(BitmapSource, int pixelX, int pixelY)`（Task 3）、`KeyEvent.ShiftDown`（Task 1）、`SelectionSession.SelectedRegion`（已有，`Int32Rect?` 虚拟物理像素）、`EditorWindow.PinRequested`（Task 4）、`Win32.VK_SHIFT`（Task 1）
- Produces: `private void OpenPin(BitmapSource image, Int32Rect? region)` — 在 `_pins` 中注册并 `Show()`；`private void TogglePinPassthrough()` — 切换全部贴屏穿透

- [ ] **Step 1: 字段与 using**

`App.xaml.cs` 顶部 using 区追加（`using EasySnipLite.Tray;` 之后）：

```csharp
using EasySnipLite.Pin;
```

类字段区追加：

```csharp
    private readonly List<PinWindow> _pins = new();
```

- [ ] **Step 2: 热键判定（Ctrl+Shift+P）**

`OnKey` 方法改为：

```csharp
    /// <summary>钩子线程事件；双击判定后切回 UI 线程启动截图。</summary>
    private void OnKey(KeyEvent evt)
    {
        if (_chord.HandleKey(evt))
        {
            Dispatcher.BeginInvoke(StartCapture);
            return;
        }
        // 贴屏穿透全局热键：Ctrl+Shift+P（穿透后鼠标点不到窗口，此为恢复手段）
        if (evt.Type == KeyEventType.KeyDown && evt.VirtualKey == 0x50 /*VK_P*/
            && evt.CtrlDown && evt.ShiftDown)
        {
            Dispatcher.BeginInvoke(TogglePinPassthrough);
        }
    }

    /// <summary>切换全部贴屏窗口穿透（统一开关）。</summary>
    private void TogglePinPassthrough()
    {
        if (_pins.Count == 0) return;
        var next = !_pins[0].IsPassthrough;
        foreach (var pin in _pins) pin.IsPassthrough = next;
    }
```

- [ ] **Step 3: StartCapture 携带选区坐标**

`StartCapture` 的 `Completed` 回调改为（在 `FinishSession()` 前先读 `SelectedRegion`——session 仍在存活期）：

```csharp
        session.Completed += result =>
        {
            var region = session.SelectedRegion; // 先取区域(物理像素)再释放会话
            FinishSession(); // 先关闭全屏 Topmost 遮罩，否则会挡住编辑器
            // 延迟到事件处理之外再打开编辑器：在按键事件栈上关闭遮罩窗口后开模态循环会崩溃
            Dispatcher.BeginInvoke(() => OpenEditor(result, region));
        };
```

- [ ] **Step 4: OpenEditor 实例化 + 贴屏接线 + OpenPin**

`OpenEditor` 改为实例方法并接线：

```csharp
    /// <summary>Enter/双击确认 → 打开标注编辑器（M3）。托盘应用无主窗口，模态独立显示。</summary>
    private void OpenEditor(BitmapSource image, Int32Rect? region)
    {
        var editor = new Editor.EditorWindow(image);
        editor.PinRequested += pinned => OpenPin(pinned, region);
        editor.ShowDialog();
    }

    /// <summary>打开贴屏窗口：位置=截图原位置(物理像素)，1:1 尺寸；region 缺失时屏幕居中兜底。</summary>
    private void OpenPin(BitmapSource image, Int32Rect? region)
    {
        int x, y;
        if (region is { } r)
        {
            x = r.X;
            y = r.Y;
        }
        else
        {
            var work = SystemParameters.WorkArea;
            x = (int)Math.Round(work.Left + (work.Width - image.PixelWidth) / 2.0);
            y = (int)Math.Round(work.Top + (work.Height - image.PixelHeight) / 2.0);
        }

        var pin = new PinWindow(image, x, y);
        pin.Closed += (_, _) => _pins.Remove(pin);
        _pins.Add(pin);
        pin.Show();
    }
```

- [ ] **Step 5: 构建与测试**

Run: `dotnet build EasySnipLite.slnx`
Expected: 0 错误 0 警告

Run: `dotnet test EasySnipLite.slnx`
Expected: 121/121 全绿（新增 PinMath 10 个）

- [ ] **Step 6: 提交**

```bash
git add src/EasySnipLite/App.xaml.cs
git commit -m "feat: App 贴屏装配(OpenPin/多窗口列表/Ctrl+Shift+P 穿透热键/截图区域传递)"
```

---

### Task 6: 单实例 Mutex + 托盘清理

**Files:**
- Modify: `src/EasySnipLite/App.xaml.cs`（`OnStartup` 开头、`StartLongCapture` 删除、`_tray` 接线）
- Modify: `src/EasySnipLite/Tray/TrayIconService.cs`（删除长截图菜单与事件）

**Interfaces:**
- Consumes: `System.Threading.Mutex`
- Produces: 无（装配行为变化：二次启动提示后退出；托盘菜单 = 区域截图/退出）

- [ ] **Step 1: TrayIconService 移除长截图入口**

`src/EasySnipLite/Tray/TrayIconService.cs`：

- 删除事件声明 `public event Action? LongCaptureRequested;`
- 删除菜单项行：`menu.Items.Add("滚动长截图", null, (_, _) => LongCaptureRequested?.Invoke());`
- 类注释更新为：`/// 托盘常驻图标（WinForms NotifyIcon 互操作）。菜单：区域截图 / 退出。`

- [ ] **Step 2: App 移除长截图托盘接线与 StartLongCapture**

`App.xaml.cs`：

- 删除 `_tray.LongCaptureRequested += StartLongCapture;`
- 删除整个 `StartLongCapture` 方法（M4 已跳过；`OpenStitch`/`--longcapture` 命令行入口保留——verify-m4.ps1 依赖）

- [ ] **Step 3: 单实例 Mutex**

`App.xaml.cs` 字段区追加：

```csharp
    private Mutex? _mutex; // 单实例，持有引用防 GC
```

`OnStartup` 开头（`base.OnStartup(e);` 之后、任何服务创建之前）插入：

```csharp
        // 单实例：二次启动提示后退出（M5）
        _mutex = new Mutex(true, "EasySnipLite_SingleInstance", out bool createdNew);
        if (!createdNew)
        {
            MessageBox.Show("EasySnipLite 已在运行。", "EasySnipLite",
                MessageBoxButton.OK, MessageBoxImage.Information);
            Shutdown();
            return;
        }
```

`OnExit` 中追加释放：

```csharp
        _mutex?.Dispose();
```

- [ ] **Step 4: 构建与测试**

Run: `dotnet build EasySnipLite.slnx`
Expected: 0 错误 0 警告

Run: `dotnet test EasySnipLite.slnx`
Expected: 121/121 全绿

- [ ] **Step 5: 提交**

```bash
git add src/EasySnipLite/App.xaml.cs src/EasySnipLite/Tray/TrayIconService.cs
git commit -m "feat: 单实例 Mutex(二次启动提示后退出)+托盘移除滚动长截图入口(M4 已跳过)"
```

---

### Task 7: 验证 + 文档同步 + 收尾

**Files:**
- Modify: `docs/PROGRESS.md`（M5 完成记录 + 踩坑）
- Modify: `CLAUDE.md`（现状一节更新 M5）
- Modify: `README.md`（功能/里程碑表更新 M5）

- [ ] **Step 1: 全量测试确认**

Run: `dotnet test EasySnipLite.slnx`
Expected: 121/121 全绿

- [ ] **Step 2: 手工 E2E 验证（设计文档第 7 节清单）**

Run: `start bin/Debug/net10.0-windows/win-x64/EasySnipLite.exe`（托盘图标出现后按清单走查）：
1. Ctrl+双击空格 → 框选 300x200 → Enter 进编辑器 → 画个矩形 → 点「贴到屏幕」：编辑器关闭，贴屏窗口出现，**物理尺寸 300x200**、**位于截图原位置**
2. 左键拖动贴屏窗口位置
3. 右键 → 「鼠标穿透」勾选：点击贴屏区域应透传到下层窗口；按 **Ctrl+Shift+P**：穿透关闭恢复可交互
4. 右键 → 透明度 85%/50%：贴屏窗口半透明
5. Ctrl+滚轮：缩放 50%~300%；右键「100% 缩放」还原
6. 再截图贴一张：两张贴屏重叠，点击其中一张它置顶
7. 再次启动 exe：弹「已在运行」提示后退出
8. 托盘右键：只有 区域截图/退出，无「滚动长截图」

- [ ] **Step 3: 同步文档**

- `CLAUDE.md`「现状」一节：M0-M4 后追加 `M5 贴屏+托盘 ✅（单实例/贴屏窗口/编辑器入口）`；「目录结构」加 `Pin/` 行；「常用命令」如无变化不动
- `README.md`：功能表「贴在屏幕」标记 ✅（如有里程碑表同步 M5）
- `docs/PROGRESS.md`：M5 从「接下来要做什么」表格移到「已完成」节，按 M3 的格式记录交付物、新增/修改文件、踩坑（如 E2E 中发现）、单测数 121/121；「接下来」表格更新 M6 为下一个

- [ ] **Step 4: 最终提交**

```bash
git add docs/PROGRESS.md CLAUDE.md README.md
git commit -m "docs: M5 贴屏+托盘完成记录(单测 121/121,手工 E2E 全过)"
```

- [ ] **Step 5: 推送并创建 PR（停在此处等用户手动合并）**

Run: `git push -u origin feature/m5-pin-tray`

Run: `gh pr create --title "M5 贴屏+托盘+单实例" --body "<PR 说明：功能清单 + 单测数 + 手工验证结果>"`
然后**停止**，明确告知用户：PR 已创建，请手动合并；合并后我会（按规则 3）清除本地分支 `feature/m5-pin-tray`（远程保留）。
